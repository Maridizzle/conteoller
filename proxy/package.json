{
  "name": "lovense-jogwheel-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Long-distance Lovense jog-wheel control proxy (holds the developer token, relays commands, serves the panel).",
  "type": "commonjs",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0"
  }
}
